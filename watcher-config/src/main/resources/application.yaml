eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://localhost:8761}/eureka/

server:
  port: ${PORT:2222}

spring:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update

